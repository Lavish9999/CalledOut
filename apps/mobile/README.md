# CalledOut Mobile

Expo/React Native client for CalledOut. Use the repository-root `README.md` for setup, deployment, and architecture instructions.

```bash
cp .env.example .env
npm install
npm run start
```

Camera, Apple sign-in, notifications, and RevenueCat require an Expo development build rather than Expo Go.
