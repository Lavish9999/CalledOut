# CalledOut full scan — July 21, 2026

## Scope

Reviewed the mobile navigation, all user-facing screens, Supabase client calls, proof upload/verification flow, redemption flow, settings, RevenueCat identity handling, notification registration, Edge Function authentication, database retry safety, lint, type safety, tests, and the admin production build.

## Fixed in this pass

- Added consistent iOS-style back navigation to pushed and modal screens.
- Changed streak wording to natural day-based copy.
- Split Today into On the clock, Later today, Needs attention, and Completed today.
- Prevented proof submission before a proof window opens.
- Added retry copy and behavior for rejected proof while the window remains open.
- Hid the duplicate header add button in Today’s empty state.
- Added tappable commitment detail pages with proof score, time, and redemption outcome.
- Masked circle invite codes until the owner taps to reveal them.
- Removed duplicate Redemption workout verification entries from circle activity.
- Reduced Profile metric-card height and exposed longest streak in the summary.
- Added explicit error states to Today, The Wall, Circles, Profile, and History.
- Added strict form validation for commitment hours/duration, circle names, and invite codes.
- Corrected timezone handling for the Today query.
- Stopped treating any available GPS reading as proof of an approved location.
- Removed orphaned proof media when database creation fails.
- Fixed reaction inserts to include the signed-in user ID.
- Fixed RevenueCat identity switching when multiple accounts sign in on one device.
- Hardened session bootstrap so a profile or purchase error cannot leave the app spinning forever.
- Added the EAS project ID when registering Expo push tokens and now surface database token-save errors.
- Made verification retries idempotent by deduplicating and uniquely indexing checks/activity.
- Prevented duplicate circle activity for redemption verification.
- Included grace-period time in server verification-window scoring.
- Hardened account-deletion authentication and database error handling.
- Made queued proof photos durable across app restarts and reused the same submission ID on retry.
- Added a recoverable account-loading error state instead of misrouting users into onboarding.
- Replaced unimplemented Pro promises with benefits that are actually enforced by the backend.

## Validation

- Mobile TypeScript: passed
- Admin TypeScript: passed
- Expo lint: passed
- Unit tests: 10/10 passed
- Admin production build: passed
- Dependency audit: 0 critical, 0 high, 11 moderate; current findings are in Expo build tooling/transitive packages, including the upstream uuid/xcode chain. No force-upgrade was applied because it would risk breaking the Expo SDK compatibility set.

## Native-build follow-up

The app still declares location, photo-library, audio, and some unused Expo packages in native configuration/dependencies. Removing those cleanly should be done with the next full EAS build rather than an OTA update. The runtime no longer treats unvalidated GPS availability as an approved location.
